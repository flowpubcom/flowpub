import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuración base. Las cabeceras de seguridad, imágenes remotas (avatares
  // de Supabase/Google) y rewrites se añadirán en sus fases.
};

export default nextConfig;
